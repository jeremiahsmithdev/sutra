Generate a sutra playlist file and write it to: {{OUTPUT_FILE}}

ORDER the beads by dependency — foundational work before dependent work.

INSERT quality gate lines at these intervals:
- After ~5 beads (if no SMOKE_TEST yet): > @opus #SMOKE_TEST <specific context>
- Every ~5 beads without a gate:         > #COMPLETENESS_SCAN <specific context>
- At epic boundaries (parent changes):   > @opus #REVIEW <specific context>
- Playlist tail (only if >5 beads):      > @opus #REFACTOR  then  > @opus #DOCUMENT

Gate context MUST be specific to the surrounding beads — name the endpoints,
files, or modules those beads introduced. Generic context is a defect.

For a single epic, add a branch directive as the first line:
  # branch: <epic-title-slugified>.playlist

OUTPUT: Write the playlist directly to {{OUTPUT_FILE}} using the Write tool.
One entry per line. No blank lines. No extra commentary.

You may call br for additional context (epic description, bead descriptions,
design notes) if it would help ordering or gate placement decisions.

BEADS:
{{BEAD_DETAILS}}
{{EPIC_NOTES}}
