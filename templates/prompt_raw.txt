You are performing a maintenance task between automated bead implementations
in the sutra autonomous loop.

## Task
{{PROMPT_TEXT}}
{{PRIOR_TASK_CONTEXT}}
{{PLAYLIST_PROGRESS}}
## Context
* Working directory: {{WORKING_DIR}}
* Branch: {{CURRENT_BRANCH}}
* Recent commits:
{{RECENT_COMMITS}}

## Rules
1. **Use `$SUTRA_DEV_PORT` for every dev server, listener, and curl.** Bind to and target `localhost:$SUTRA_DEV_PORT` (env var, set by sutra for this session). Never hardcode `:5000`, `:8080`, `:3000`, etc. — another worktree's app may be on the default port and silent cross-contamination is the failure mode this prevents. The literal string `:8080` (or `:5000`, `:3000`) MUST NOT appear in any command you run when `$SUTRA_DEV_PORT` is set.
2. Search the codebase before assuming anything about structure.
3. Make targeted fixes and commit with conventional commit messages.
4. You may update beads using br update/br create/br close as needed.
5. Do NOT touch .beads/ files directly — use br commands only.
6. Do NOT run br sync — the outer loop handles that.
7. When done, just exit. There is no bead to close for this task.
8. Prefer Read, Grep, and Glob tools for file operations. Only spawn Agent sub-tasks when exploring unfamiliar parts of the codebase that are not described in this task or the project context. Direct tool use is faster and cheaper.
{{FILE_MAP}}
## Closing Out

Your final assistant message becomes the next agent's `## Prior Task Context` and is also what a human reviewer skims in the session log. Give it two clearly-labeled parts:

**Summary** — 2–4 lines on what you did. The diff carries the details; this is the orientation line the orchestrator and reviewer read first.

**Handoff** — Brief the next agent like a capable colleague picking up where you left off. The next playlist item is:

{{NEXT_TASK_HINT}}

Transmit what you now know that you didn't when you started — the architecture you understand, the invariants you trust, the judgment calls you made. Use file:line pointers where they help. Be generous with what genuinely helps the next task, ruthless about what doesn't.
