You are performing a maintenance task between automated bead implementations
in the ralph autonomous loop.

## Task
{{PROMPT_TEXT}}
{{PRIOR_TASK_CONTEXT}}
{{PLAYLIST_PROGRESS}}
## Context
* Working directory: {{WORKING_DIR}}
* Branch: {{CURRENT_BRANCH}}
* Recent commits:
{{RECENT_COMMITS}}

## Rules
1. **Use `$RALPH_DEV_PORT` for every dev server, listener, and curl.** Bind to and target `localhost:$RALPH_DEV_PORT` (env var, set by ralph for this session). Never hardcode `:5000`, `:8080`, `:3000`, etc. — another worktree's app may be on the default port and silent cross-contamination is the failure mode this prevents. The literal string `:8080` (or `:5000`, `:3000`) MUST NOT appear in any command you run when `$RALPH_DEV_PORT` is set.
2. Search the codebase before assuming anything about structure.
3. Make targeted fixes and commit with conventional commit messages.
4. You may update beads using br update/br create/br close as needed.
5. Do NOT touch .beads/ files directly — use br commands only.
6. Do NOT run br sync — the outer loop handles that.
7. When done, just exit. There is no bead to close for this task.
8. Prefer Read, Grep, and Glob tools for file operations. Only spawn Agent sub-tasks when exploring unfamiliar parts of the codebase that are not described in this task or the project context. Direct tool use is faster and cheaper.
{{FILE_MAP}}
## Handoff
Your final assistant message becomes the next agent's `## Prior Task Context`. Treat it as a deliberate handoff, not a status report.

The next playlist item is:
{{NEXT_TASK_HINT}}

Prioritise context the next agent CANNOT recover from `git log` / `git diff` / `br show`: architecture or invariants you discovered through file exploration (with file:line pointers), decisions you made on ambiguous points and why, surprises (code that doesn't behave as its name suggests, hidden coupling, dead branches), and tentative suggestions for the next task based on what you now know — flag concerns, shortcuts, or pitfalls. Skip restating what you changed; the diff shows that.
